spring:
  datasource:
    url: jdbc:tc:mariadb:11.8:///test
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: never
  ai:
    google.genai:
      api-key: test-gemini-api-key
      embedding.api-key: test-gemini-api-key
    vectorstore.mariadb:
      schema-validation: true
      dimensions: 768
      schema-name: test

batch:
  post-sync.cron: "-"
  excerpt-refresh.cron: "-"

notion:
  datasource-id: test-notion-database-id
  token: test-notion-token

gemini.api-key: test-gemini-api-key